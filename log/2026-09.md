# Event Log — 2026-09

> 事件流水帳，由 git hooks 和 AI Agent 共同維護。
> **嚴禁修改已有條目**，只能 append。
> 詳見 `log/README.md`。

## [2026-09-09 21:45] feature | 新增 get_view_graphics_diagnostics（視圖表現法唯讀診斷）

- actor: claude-opus-5[1m] (via Claude Code / VS Code)
- files: MCP/Core/Commands/CommandExecutor.ViewGraphicsDiagnostics.cs, MCP/Core/CommandExecutor.cs, MCP-Server/src/tools/visualization-tools.ts, CLAUDE.md, README.md, README.zh-TW.md, docs/DOCUMENT_AUDIENCE_INVENTORY.md, docs/BIM_MCP/**（工具數 192→193 連動）
- trigger: manual
- summary: 新增第 193 個工具，唯讀回答「這張圖為什麼長這樣」——沿 Revit 覆寫優先序（視覺型式 > 元素覆寫 > 篩選器 > 類別覆寫 > 材料）一次掃完並回報所有命中成因。

- **動機**：接手他人（同事、外部顧問）的模型時，看到不合預期的表現法，只能到 VG、篩選器、視圖樣板、材料、元素覆寫逐層點開比對，成本高且容易漏掉「同時有兩個來源」的情況。此工具把這段人工排查一次做完。

- **Verdict 設計（本次審查後改掉的關鍵點）**：初版遇到 Shaded/Realistic 視覺型式就直接 return 單一字串，後續的元素覆寫、篩選器、材料一律不再檢查——資料雖在 payload 裡，但 AI 只會引用 Verdict，等於給出誤導結論。改為收集**所有**命中成因，依優先序排成 `Causes[]`，每筆帶 `Priority` / `Source` / `Scope`（整個視圖／這個元素／整個品類／全專案）/ `Detail` / `Fix`，另附 `PrimaryCause` 與人類可讀 `Summary`。

- **偵測範圍擴大**：初版只認「表面前景／背景填滿顏色」，Halftone、透明度、線色、線寬造成的差異會落到「都沒找到」。現在 `DescribeDiagEffects()` 涵蓋表面／剖面前景背景填滿（顏色與樣式）、投影線色、剖面線色、線寬、半色調、透明度；另外新增兩個「元素根本看不到」的成因：篩選器把可見性關掉、品類在本視圖被關閉。

- **唯讀保證**：不開 Transaction，只呼叫 `View.GetElementOverrides` / `GetCategoryOverrides` / `GetFilters` / `GetFilterOverrides` 與材料屬性的 getter。工具名以 `get_` 開頭，`annotations.ts` 的 READONLY_PREFIXES 自動判為 `readOnlyHint=true`，無須新增覆寫條目。

- **驗收**：`dotnet build` R22 / R24 / R25 / R26 皆 0 errors（跨 `REVIT2025_OR_GREATER` 的 Int32/Int64 `ElementId` 分歧已驗）；`verify-qaqc.ps1 -SkipDeploy` 全量 PASS 72 / FAIL 0 / WARN 2（兩個 WARN 皆為既有項目：3-5 working-tree 提示、check_sanitary_fixture_requirements 隔離）；7-14 確認 193 個工具各有一張 tools-index 卡片，7-15 確認 dispatcher case 對得上。

- **未驗（誠實陳述）**：尚未在真實 Revit session 上實跑此工具。Verdict 的啟發式判斷——尤其多成因同時命中時的排序是否符合 Revit 實際的勝出規則——僅以 API 語意為據，無實機證據。另未涵蓋連結模型覆寫、設計選項、階段化圖形覆寫三個來源，目前只在「都沒找到」時以文字提示使用者自行檢查。

## [2026-09-16 17:48] fix | 3D 元素定位改用 Section Box
- actor: gpt-5 (via Codex desktop)
- files: MCP/Core/Commands/CommandExecutor.ViewCropBox.cs, MCP-Server/src/tools/view-cropbox-tools.ts
- trigger: manual
- summary: 修正 align_view_cropbox_to_element：3D 視圖改用 View3D.SetSectionBox() 做 XYZ 三向裁切並關閉 CropBox；2D 視圖維持原 CropBox 行為。

## [2026-09-16 17:58] feature | 新增獨立 set_3d_section_box 工具
- actor: gpt-5 (via Codex desktop)
- files: MCP/Core/CommandExecutor.cs, MCP/Core/Commands/CommandExecutor.ViewCropBox.cs, MCP-Server/src/tools/view-cropbox-tools.ts, CLAUDE.md, README.md, README.zh-TW.md, docs/DOCUMENT_AUDIENCE_INVENTORY.md, docs/BIM_MCP/**
- trigger: manual
- summary: 延續上一筆並修正工具邊界：新增第 194 個 MCP 工具 set_3d_section_box，使用 View3D.SetSectionBox() 依元素完整 XYZ BoundingBox 加上三向 padding；原 align_view_cropbox_to_element 限定為 2D，遇 3D 會明確拒絕。另提供 clearCropOnly 清理先前誤啟用的 3D CropBox。R24 C# 與 MCP Server build 皆 0 errors；QA/QC 67 PASS / 0 FAIL / 2 WARN / 3 SKIP。Revit 仍在執行，尚未部署及實機呼叫。

## [2026-09-16 17:59] deploy | 部署 set_3d_section_box 至 Revit 2024
- actor: gpt-5 (via Codex desktop)
- files: %APPDATA%/Autodesk/Revit/Addins/2024/RevitMCP.addin, %APPDATA%/Autodesk/Revit/Addins/2024/RevitMCP/*
- trigger: manual
- summary: 使用 install-addon.ps1 -Version 2024 -NonInteractive 部署 13 個 DLL 與 manifest，逐檔 SHA256 通過；部署後 QA/QC 73 PASS / 0 FAIL / 2 WARN / 4 SKIP，RevitMCP.dll 與 Release.R24 build output hash 一致。

## [2026-09-16 18:09] fix | 3D Section Box 舊工具名相容入口
- actor: gpt-5 (via Codex desktop)
- files: MCP/Core/Commands/CommandExecutor.ViewCropBox.cs, MCP-Server/src/tools/view-cropbox-tools.ts, docs/BIM_MCP/reference/tools-index.html, %APPDATA%/Autodesk/Revit/Addins/2024/RevitMCP/*
- trigger: manual
- summary: 因既有 Codex task 的 MCP tool registry 快取重啟前 schema，align_view_cropbox_to_element 在目標為 View3D 時改為直接轉交 Set3DSectionBox；2D 才保留 CropBox 路徑。R24 與 MCP Server build 0 errors，重新部署 13 DLL，QA/QC 73 PASS / 0 FAIL / 2 WARN / 4 SKIP，deployed DLL hash 與 Release.R24 一致。

## [2026-09-16 18:31] docs | 排煙 Skill 新增天花板高度來源路由
- actor: gpt-5 (via Codex desktop)
- files: .claude/skills/smoke-exhaust/SKILL.md, domain/smoke-exhaust-review.md
- trigger: manual
- summary: 依模型成熟度路由 ceilingHeightSource：天花未建或部分建模時用 room_parameter，完整逐房覆蓋時才用 ceiling_element；檢討與 Excel 匯出須使用相同來源。另新增 Room 高度前置驗證，禁止把口述 3000 mm 覆蓋模型實值。實測 2F：A2-02=3000 mm、A2-03/A2-06=2438.4 mm。skill quick_validate 通過；QA/QC 67 PASS / 0 FAIL / 2 WARN / 3 SKIP。

## [2026-09-16 20:10] fix | Section Box 支援非對稱 padding
- actor: gpt-5 (via Codex desktop)
- files: MCP/Core/Commands/CommandExecutor.ViewCropBox.cs, MCP-Server/src/tools/view-cropbox-tools.ts, docs/BIM_MCP/reference/tools-index.html
- trigger: manual
- summary: 在 A1-09 實測發現房間底標高約 -2848 mm，而 1F 樑底達 -5348 mm；原統一 600 mm padding 只顯示樑上段，且同時向上切入 2F 樑。新增 padding_xy_mm、padding_bottom_mm、padding_top_mm，並可用 includeSupportingBeams 自動將與目標底面相接、XY 相交的完整樑 BoundingBox 納入，避免用任意水平 padding 猜測；保留 padding_mm 相容入口。R22/R23/R24/R25/R26 與 MCP Server 均編譯成功；Revit 仍開啟使新版 DLL 無法覆蓋，未宣稱新版已完成 live reload 驗證。

## [2026-09-16 20:18] fix | 排煙窗型係數改為可稽核專案政策
- actor: gpt-5 (via Codex desktop)
- files: MCP/Core/Commands/CommandExecutor.SmokeExhaust.cs, MCP-Server/src/tools/smoke-exhaust-tools.ts, .claude/skills/smoke-exhaust/SKILL.md, domain/smoke-exhaust-review.md
- trigger: manual
- summary: 正式名稱統一為推射／外推、推開／平開、橫拉、固定；新增三種開啟係數與未知窗型假設參數，檢討結果回傳 WindowOperationPolicy，Excel 匯出沿用同一政策。本案採推射=1.0、平開=0.5、橫拉=0.5、固定=0、未知暫按推射且保留人工確認。smoke-exhaust Skill quick_validate 通過，QA/QC 67 PASS / 0 FAIL / 2 WARN / 3 SKIP。

## [2026-09-17 16:39] validation | A1-09 真正 Section Box 實機驗證
- actor: gpt-5 (via Codex desktop)
- files: %APPDATA%/Autodesk/Revit/Addins/2024/RevitMCP/*, log/2026-09.md
- trigger: manual
- summary: Revit 2024 重新啟動後，以既有相容入口在「排煙檢討_1F_A1-09_納骨室1F-A」實跑新版 View3D.SetSectionBox；結果為 Mode=SectionBox、CropBoxActive=false、SectionBoxActive=true，水平 padding 600 mm、上下 padding 0 mm，自動納入 7 支當層支承樑。Section Box Z 範圍為 -5348 至 152 mm，底面涵蓋完整支承樑且頂面不向上一層外擴；作用中視圖、房間與視圖診斷均重新查詢確認。部署版與 Release.R24 RevitMCP.dll SHA256 一致。

## [2026-09-17 17:00] feat | Section Box 上層結構兩路由
- actor: claude-opus-5 (via Claude Code VSCode)
- files: MCP/Core/Commands/CommandExecutor.ViewCropBox.cs, MCP-Server/src/tools/view-cropbox-tools.ts, docs/BIM_MCP/reference/tools-index.html, domain/smoke-exhaust-review.md, .claude/skills/smoke-exhaust/SKILL.md
- trigger: manual
- summary: set_3d_section_box（及轉交的 align_view_cropbox_to_element）新增 upperStructureMode：current_level_only（預設，行為不變）／include_upper_structure_without_slab（頂面＝最近上層樓板底 − upperSlabClearance_mm，預設 10 mm，上層樑可見、樓板不含）。找不到上層樓板直接報錯不改視圖；回傳 UpperStructure 診斷（樓板 ID、底/頂高、上層樑數、TopBelowTargetTop）。抽出 TryGetWorldBounds 共用。R24、R26、MCP Server 編譯 0 錯誤；QA/QC 未跑（本 session PowerShell 啟動即 AccessViolation 崩潰）；Revit 執行中未部署，尚未實機驗證。

## [2026-09-18 13:49] session-summary | Section Box 上層結構兩路由（已編譯已部署，未實機驗證）
- actor: claude-opus-5 (via Claude Code VSCode)
- trigger: manual (/handoff)
- summary: 為 set_3d_section_box 新增 upperStructureMode 兩條路由與 upperSlabClearance_mm，程式、文件、部署完成，但實機驗證被 MCP 409 鎖擋下，尚未 commit。

### 本 session 完成
- `set_3d_section_box`（及轉交的 `align_view_cropbox_to_element`）新增 `upperStructureMode`：`current_level_only`（預設，行為與 be49b0e 實測結果完全相同）／`include_upper_structure_without_slab`（頂面＝最近上層樓板底 − `upperSlabClearance_mm`，預設 10 mm）。
- 上層樓板判定寫在 `CommandExecutor.ViewCropBox.cs`：與目標 XY 重疊、底面高於目標底 + 容許值、頂面不低於目標頂 − 容許值，取底面最低者；容許值沿用 `supportingBeamTolerance_mm`。
- 抽出 `TryGetWorldBounds()` 共用（原支承樑 8 角點展開邏輯重複兩份）。
- 回傳新增 `UpperStructure` 診斷物件：樓板 ID／底高／頂高／候選數、上層樑數、`TopBelowTargetTop`、`PaddingTopApplied`。
- 文件：`domain/smoke-exhaust-review.md` 新增「逐房 3D Section Box 檢視」章節（v1.2→v1.3）；`.claude/skills/smoke-exhaust/SKILL.md` 步驟 3 加入路由說明與「未指定時先問使用者」；`tools-index.html` 描述同步。
- 建置：R24、R26、MCP Server 全部 0 錯誤。部署：R24 13 個 DLL 已複製到 `Addins/2024/RevitMCP/`，逐檔 binary 比對一致，舊 DLL 備份為 `RevitMCP.dll.bak-<stamp>`。
- 尚未 commit（工作樹仍有本次改動）。

### 對齊的概念
- **上層結構可見性是使用者的選擇，不是工具的預設判斷**：同一個房間檢討，有時要乾淨的當層視圖，有時要看上層樑下緣與排煙有效帶（天花板下 80 cm）的關係。因此做成顯式路由參數，預設維持已驗證的保守行為。
- **「看得到樑、看不到樓板」的實作方式是切在樓板底下方**，不是過濾類別。Section Box 是幾何裁切，頂面設在樓板底 − 10 mm，樓板本體自然落在框外，吊在樓板下的樑則完整保留。
- **找不到上層樓板＝硬錯誤，不猜高度**：樓板可能未建模、在連結模型（目前抓不到）、或 XY 未覆蓋。三種情況都直接拋例外且不修改視圖，由使用者決定先建模或改走 `current_level_only`。同理 `UpperStructureBeamCount=0` 與 `TopBelowTargetTop=true` 都必須向使用者揭露，不可宣稱「上層結構已顯示」。

### Pending（下 session 可接續）
- **實機驗證 A1-09 兩種模式**：本 session 最後 `get_active_view` 回 HTTP 409（另一個 AI 用戶端占著獨佔鎖）。需先釋放連線＋`/mcp` reconnect（reconnect 也才會載到含 `upperStructureMode` 的新 schema），再對同一房間各跑一次，比對 Section Box Z 上緣與回傳的 `UpperStructure`。
- **QA/QC 未跑**：本 session Windows PowerShell 5.1 每次啟動都 AccessViolation 崩潰（第一個 `git status` 指令就崩過，早於任何程式修改；`verify-qaqc.ps1` 跑到 Phase 1-3 崩潰，崩潰前 0 FAIL）。下次開 session 先確認 PowerShell 能跑，再補完整 QA/QC。
- **commit 與推送**：驗證通過後才 commit 本次改動到 `codex/smoke-section-review`。
- **工作樹有其他 session 的在飛改動**（非本 session 產出）：`CommandExecutor.ViewFilter.cs`（未追蹤）、`visualization-tools.ts`、`CommandExecutor.cs`、CLAUDE.md 工具數 194→197、多個 docs/README。commit 前要分清楚哪些屬於哪一批，且這批 view filter 工具在 log 裡目前沒有對應條目。
- 可刪除：`MCP-Server/build.bak-20260917-185558-upperStructure/`（建置備份，確認無用後清掉）。

### 拒絕的選項（省得重議）
- **用 `padding_top_mm` 加大數值來露出上層樑**：拒絕。樓板厚度與樓高每案不同，等於用猜的數字碰運氣，且一定會同時切進樓板或上一層空間。
- **把上層樓板用類別隱藏／圖形覆寫關掉**：拒絕。那會改視圖的可見性設定（副作用留在視圖上、影響出圖），而 Section Box 只是裁切，換模式即還原。
- **找不到上層樓板時 fallback 用樓層標高推算**：拒絕。違反「不猜高度」；樓層標高不等於樓板底（有降板、不同厚度）。
- **另開一個新工具（例如 `set_3d_section_box_with_upper`）**：拒絕。同一件事分兩個工具會讓 skill 與文件雙份維護，且 194→197 的工具膨脹已是既有壓力；用參數路由。

### 下次可接續的起手式
1. `git status` 確認本次改動仍在，並區分其他 session 的在飛檔案。
2. 確認 Revit 開啟、無其他 AI 用戶端占鎖（必要時按 Revit 功能區「切換/釋放連線」），在 Claude Code 執行 `/mcp` reconnect `revit-mcp` 讓 schema 更新到含 `upperStructureMode`。
3. `get_active_view` 重新錨定，對 A1-09 納骨室先跑 `current_level_only`（應重現 be49b0e 的 Z −5348～152 mm），再跑 `include_upper_structure_without_slab`，貼出兩次回傳的 `SectionBox_mm` 與 `UpperStructure` 比對。
4. 補跑 `verify-qaqc.ps1 -SkipBuild -SkipDeploy`，通過後 commit。

## [2026-09-18 13:49] session-summary | 視圖篩選器工具落地＋平面圖上色機制釐清
- actor: claude-opus-5 (via Claude Code VSCode)
- trigger: manual (/handoff)
- summary: 從「雨遮支撐檢討要怎麼上色」一路追到 Revit 平面圖表現法的決定性規則，再新增 3 個視圖篩選器 MCP 工具（194→197）並修復本機 PowerShell。工具已部署上線但尚未實跑。

### 本 session 完成
- 新增 3 個 MCP 工具：`get_filterable_parameters`（唯讀）／`create_view_filter`／`remove_view_filter`。新增 `MCP/Core/Commands/CommandExecutor.ViewFilter.cs`、dispatcher 3 個 case、`visualization-tools.ts` 工具定義。跨版本以 `#if REVIT2023_OR_GREATER` 處理字串規則工廠的簽章變更
- 工具數 194 → 197，同步 CLAUDE.md／README×2／DOCUMENT_AUDIENCE_INVENTORY／tools-index.html，以及 BIM_MCP 6 個頁面共 17 處殘留宣稱
- R24 編譯 0 錯誤、部署 13 個 DLL，SHA256 與 build 輸出一致
- QA/QC 完整 gate：67 PASS / 0 FAIL / 2 WARN / 3 SKIP
- 修復本機 Windows PowerShell 5.1 的 AccessViolation（見下）
- **未 commit**（依 dll-to-mcp-tool skill：實測通過才提交）

### 對齊的概念
- **平面圖元素能不能上色，由 Cut Plane 決定**——不是 Top clip、不是 underlay 設定、也不是類別。實測同一元素：Cut 在其上 → 走正常投影繪製、覆寫飽和生效；Cut 在其下（元素高於剖切面）→ 視圖不畫它，畫面上的是 underlay。抬高 Top clip 1200 mm 對畫面零影響，是明確的否定結果
- **顏色偏淡就是 underlay halftone 的指紋**——代表該元素不在視圖範圍內。halftone 層級為 Discipline(Underlay) → Model Category → Phase → Graphic Overrides → Filter → Element Override，覆寫與 halftone 是疊加而非互斥
- **underlay 的覆寫行為不可預測**：同類別、同視圖、同樣在 Top clip 之上，一片屋頂吃覆寫、另一片與梁完全不吃。未能釘死決定規則，實務結論是不要依賴 underlay 上色
- **牆柱與梁不可能在同一張平面圖同時正確**：牆柱要 Cut≈2 m、雨遮梁要 Cut≈4 m，互斥
- **逐元素 override 不是規則**——不涵蓋新元素、別人接手看不出意圖。規則式表現法應該用 View Filter 表達（使用者指出，並直接促成本次新工具）
- 匯出的 PNG 會把 Revit 的選取高亮一起畫進去，判讀截圖時要先排除

### Pending（下 session 可接續）
- **3 個新工具已上線但從未實跑**（重啟 Claude Code 後才進入工具清單；`/mcp` 重連不會加入新工具）
- 用 `get_filterable_parameters(["Walls","Structural Columns"])` 確認 `Base Constraint`／`Reference Level` 這類樓層型參數是否可作篩選規則——**本 session 懸而未決的問題，新工具就是為了回答它**
- 依結果建三個篩選器：F1 淡化/隱藏 2F 牆（Base Constraint = 2F Level）、F2 雨遮梁上色（Structural Framing + Reference Level = 2F Level）、F3 支撐牆柱上色（建議用 Comments = CANOPY-SUP 之類的資料驅動規則）
- 沙盒視圖 `2F Floor Plan Copy 1`（id 4470927）的 View Range 仍停在我改過的狀態（Bottom/View Depth → 1F Level +0），原值為 2F Level −800，尚未還原
- 實測通過後才 git commit
- 本 session 結束時 `export_view_image`／`set_view_range` 顯示為已斷線，下次需確認

### 拒絕的選項（省得重議）
- **1F 平面靠 underlay 讓梁上色**：surface 填滿、cut 填滿、投影線顏色、隱藏樓板解遮擋，四種方式全部實測失敗。這條路是死的
- **抬高 Top clip 讓梁進入視圖範圍**：+3000 → +4200 實測畫面完全沒變
- **隱藏 Floors 類別解遮擋**：實測無效，擋住 1F 牆的是正上方的 2F 牆而非樓板
- **把 Cut Plane 抬到 3.3 m 讓梁被剖到**：技術上成功（梁確實變色），但該視圖就不再是一張正常的 1F 平面，門窗表現法跑掉
- **逐個 hide 65 道 2F 牆**：使用者否決——不是規則、不涵蓋新元素、接手者看不出意圖
- **`ngen update` 修 PowerShell**：無效，它只重編「過期」映像，偵測不到「損毀」，回報 up to date 後什麼都沒做

### 環境修復紀錄
Windows PowerShell 5.1 任何需要反射 .NET 成員的指令都 AccessViolation（`DotNetAdapter.GetDotNetMethod`），`COMPLUS_ZapDisable=1` 可繞過 → 確認是 `System.Management.Automation` 原生映像損毀。以 `ngen uninstall` + `ngen install`（系統管理員）重建後根治，現已不需任何環境變數。註：診斷過程曾誤判「映像檔不存在於磁碟」，實為 NGen 對長組件名會截斷加雜湊（實際路徑 `System.Manaa57fc8cc#`），該證據不成立，但重建有效的結論不受影響。

### 下次可接續的起手式
1. `get_active_view` 重新錨定連線與作用中視圖
2. `get_filterable_parameters` 帶 `["Walls","Structural Columns"]`、`contains: "Level"` → 直接回答樓層型參數可否作規則
3. 依結果建 F1/F2/F3，每個看回傳的 `MatchedElementCount` 確認命中數非 0
4. 出圖確認後還原沙盒視圖 View Range，再 commit

## [2026-09-23 13:25] feature | 新增 10 個診斷層 MCP 工具（197 -> 207）
- actor: claude-opus-5[1m] (via Claude Code) + gpt-5.6-terra (via Codex CLI, 文件操作 4 工具)
- files: MCP/Core/Commands/CommandExecutor.{ViewCapture,FillPatternQuery,ViewRange,LinkDisplay,DocumentOps}.cs, MCP/Core/Commands/CommandExecutor.{ViewFilter,GM_GreenMaterial}.cs, MCP/Core/CommandExecutor.cs, MCP-Server/src/index.ts, MCP-Server/src/tools/{index,annotations,revit-tools,visualization-tools}.ts, MCP-Server/src/tools/{view-capture,fill-pattern,view-range,link-display,document}-tools.ts, CLAUDE.md, README.md, README.zh-TW.md, docs/DOCUMENT_AUDIENCE_INVENTORY.md, docs/BIM_MCP/**
- trigger: manual
- summary: 補上 AI 端先前完全缺席的診斷能力——capture_view_image 讓模型直接看見視圖畫面（server 端改以 image content block 回傳，否則只會拿到 base64 文字）；list_fill_patterns 讓 hatch 名稱可查不必猜；get/set_view_range 與 get/set_link_display_settings 對應「元素在模型裡卻看不到」與「主檔覆寫對連結無效」兩個最常見盲區；list_open_documents/open_document/save_document/reload_links 補齊文件層級操作。同時擴充 create_view_filter 支援 patternName/backgroundPatternName/backgroundColor（未給參數時行為不變），並修掉 get_all_materials 遇到未初始化材質顏色會整份查詢 crash 的問題。QA/QC 67 PASS 0 FAIL。

## [2026-09-23 16:18] feature | 新增 get_camera_info / get_material_graphics / set_material_graphics（207→210）
- actor: claude-opus-5-5 (via Claude Code VS Code)；文件數量同步由 claude-sonnet-5 subagent 執行
- files: MCP/Core/Commands/CommandExecutor.CameraInfo.cs, MCP/Core/Commands/CommandExecutor.MaterialGraphics.cs, MCP/Core/CommandExecutor.cs, MCP-Server/src/tools/camera-tools.ts, MCP-Server/src/tools/material-graphics-tools.ts, MCP-Server/src/tools/index.ts, CLAUDE.md, README.md, README.zh-TW.md, docs/DOCUMENT_AUDIENCE_INVENTORY.md, docs/BIM_MCP/**（計數與 tools-index 卡片）
- trigger: manual
- summary: 相機 Eye Elevation 顯示負值無法判斷基準 → get_camera_info 讀 ViewOrientation3D 眼睛座標並反推參數 0 點；視圖篩選器／V/G 只作用單一視圖 → set_material_graphics 直接改材質著色顏色與表面/切割填充，所有視圖生效。R24/R26 編譯 0 錯誤，R24 已部署（SHA256 驗證通過），verify-qaqc PASS 67 / FAIL 0。尚未對真實模型實跑驗證。
- note: create_view_filter 的 patternMode 預設 auto 在平面圖會落到「切割」，要改地形等投影表面須明確給 patternMode=surface。

## [2026-09-23 17:05] feature | 新增 close_document（210→211）
- actor: claude-sonnet-5 (via Claude Code subagent)
- files: MCP-Server/src/tools/document-tools.ts, MCP/Core/Commands/CommandExecutor.DocumentOps.cs, MCP/Core/CommandExecutor.cs, CLAUDE.md, README.md, README.zh-TW.md, docs/DOCUMENT_AUDIENCE_INVENTORY.md, docs/BIM_MCP/reference/tools-index.html, docs/BIM_MCP/index.html, docs/BIM_MCP/shared.js, docs/BIM_MCP/reference/{architecture-v2,domain-index,personal-llm-wiki,philosophy-22-propositions,skills-index}.html
- trigger: manual
- summary: 補上 open_document/save_document 的收尾動作——某連結檔被獨立開啟編輯後，宿主文件要重新載入該連結前必須先關閉它，先前沒有對應工具。依 documentTitle 比對關閉非連結文件；Document.Close() 對作用中文件會擲例外（Revit API 限制），故明確擋下並提示需先切換視圖；連結文件同樣拒絕，須改由宿主文件的連結管理處理；文件已修改時預設拒絕關閉，除非 save=true 先存檔或 discardChanges=true 放棄變更。annotations 走預設分類（無 close_ 前綴規則，落在非破壞性寫入的預設值，剛好符合 readOnlyHint=false/destructiveHint=false 的需求，未動 annotations.ts）。R24 編譯 0 錯誤（未部署，Revit 執行中）；verify-qaqc -SkipBuild -SkipDeploy PASS 67 / FAIL 0 / WARN 2（皆既有項目：3-5 working-tree 提示、check_sanitary_fixture_requirements 隔離）。

## [2026-09-23 18:40] fix | 排煙檢討 projectedOpeningRatio 預設改為 1.0
- actor: claude-opus-5-5 (via Claude Code VS Code)
- files: MCP-Server/src/tools/smoke-exhaust-tools.ts, MCP/Core/Commands/CommandExecutor.SmokeExhaust.cs, domain/smoke-exhaust-review.md
- trigger: manual
- summary: 依使用者指示，推射／外推／上懸窗有效面積係數預設由 0.5 改為 1.0（check_smoke_exhaust_windows、export_smoke_review_excel 兩工具 schema 與 C# 預設同步），domain 窗型表改標「1.0（工具預設）」。tsc 通過、R24 編譯 0 錯誤；未部署、未實機。

## [2026-09-23 18:55] docs | 推開／平開窗係數統一為 1.0
- actor: claude-opus-5-5 (via Claude Code VS Code)
- files: domain/smoke-exhaust-review.md, .claude/skills/smoke-exhaust/SKILL.md, MCP-Server/src/tools/smoke-exhaust-tools.ts
- trigger: manual
- summary: 依使用者指示，推開／平開窗有效面積係數統一為 1.0。程式預設原本即為 1.0，此次修正 domain 窗型表（本案 0.5 → 1.0 工具預設）、smoke-exhaust skill 範例與係數表、TS 參數說明文字，消除文件與工具的不一致。
